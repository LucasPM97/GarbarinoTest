<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowContentTransitions">true</item>
    </style>

    <style name="Divider">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">1dp</item>
        <item name="android:background">?android:attr/listDivider</item>
        <item name="android:layout_marginBottom">5dp</item>
        <item name="android:layout_marginTop">5dp</item>
    </style>

    <style name="RatingBarIndicator" parent="Widget.AppCompat.RatingBar.Indicator">
        <item name="android:progressBackgroundTint">@color/colorRatingStarsBackground</item>
        <item name="android:progressTint">@color/colorRatingStarsTint</item>
        <item name="android:secondaryProgressTint">@color/colorRatingStarsTint</item>
    </style>

    <style name="RatingBarSmall" parent="Widget.AppCompat.RatingBar.Small">
        <item name="android:progressBackgroundTint">@color/colorRatingStarsBackground</item>
        <item name="android:progressTint">@color/colorRatingStarsTint</item>
        <item name="android:secondaryProgressTint">@color/colorRatingStarsTint</item>
    </style>

</resources>
